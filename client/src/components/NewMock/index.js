import NewMockForm from './NewMock';
import './styles.css';

const NewMock = () => {
    return (
        <div className='new-mock-container'>
            <NewMockForm />
        </div>
    );
};

export default NewMock;
